{
  "name": "places-lead-finder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:arch": "node scripts/lint-arch.mjs",
    "dev": "tsx --env-file=.env src/main.ts",
    "smoke": "tsx --env-file=.env scripts/smoke-sweep.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "fastify": "^5.11.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
